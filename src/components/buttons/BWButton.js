import * as React from 'react';
import Button from '@mui/material/Button';

export default function BWButton(props){
  return (
    <Button color ="primary" variant="contained">{props}</Button>
  );
}

